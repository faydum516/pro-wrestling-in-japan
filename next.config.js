/** @type {import('next').NextConfig} */
const nextConfig = {
    /**experimental: {
        appDir: true
    },**/
    images: {
        domains: ['faydum516.github.io']
    }
}

module.exports = nextConfig
